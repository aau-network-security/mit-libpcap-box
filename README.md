# Master of IT (IT-vest): libpcap container #

This is a quick and dirty docker container for the tools used during
the IT Master course at Aalborg University.

## Usage: ##

    docker run -it --net=host --privileged mit-libpcap-box
